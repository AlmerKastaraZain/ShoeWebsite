const blackCoverElement = document.getElementById('blackCover')

function ShowBlackcover() {
    blackCoverElement.classList.remove('invisible');
}

function HideBlackcover() {
    blackCoverElement.classList.add('invisible');
}